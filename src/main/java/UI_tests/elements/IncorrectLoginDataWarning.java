package UI_tests.elements;

import org.openqa.selenium.WebDriver;

public class IncorrectLoginDataWarning extends CompositeElement {

    protected IncorrectLoginDataWarning(WebDriver driver) {
        super(driver);
    }
}
